# Re-evaluation — ahs

2 change(s) on the site itself; 2 change(s) in what we were able to check, which is about our coverage rather than the business.

Historical research is unchanged. This is a comparison beside it, not a replacement for it.

## Changes on the site

- `website`: not checked → present (newly_observed)
- `whatsapp`: not_found → present (contradicted)

## Changes in what we could check

These are about our own coverage. None of them is a statement about the business.

- `ai_visibility`: not checked → unverified (now_unverified)
- `page_speed`: present → unverified (now_unverified)

## Unchanged (7)

`arabic`, `blog`, `blog_cadence`, `click_to_call`, `https`, `portfolio_depth`, `social_proof`
