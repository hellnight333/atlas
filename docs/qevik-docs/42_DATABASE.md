# Database

PostgreSQL is used by DB-backed tests.

Latest state:
- PostgreSQL unavailable
- port 5432 refused
- 20 failures + 87 collection errors attributed to DB availability

Action:
restore PostgreSQL and rerun the full suite.

Atlas database/schema rename remains deferred.
