# MIGRATION_RISK_REGISTER

Phase 0 deliverable 5. Likelihood/impact are the author's estimates (INFERRED)
unless a row cites an observed occurrence; the **Evidence** column is what is
PROVED/OBSERVED. Rollback implication answers: "if this happens after cutover,
does the documented rollback (Cloudflare origin back to `2.28.62.83`) still
work?"

Scale: L = Low / M = Medium / H = High.

## A. Data integrity and state

| ID | Risk | Component | L | I | Detection | Mitigation | Rollback implication | Evidence |
|---|---|---|---|---|---|---|---|---|
| R-01 | Final delta sync misses writes because services on the old host keep running (workers claim missions every 10 s; publish worker writes `/srv/sites`) | DB `qevik`, `/srv/sites`, `/var/lib/qevik` | M | H | Row counts + `pg_stat_database.xact_commit` frozen before/after; `find -newer` marker on file trees | Phase 9 stops all seven `qevik-*` units and both timers **before** the final dump; verify no `qevik` backends remain in `pg_stat_activity`; only then dump + rsync | Old host is untouched until decommission → rollback restores the old truth; any writes made on the target after cutover are lost on rollback (accept, or reverse-sync — owner decision) | 5 workers `--interval 10` (PROVED units); `/srv/sites` mtime 2026-08-27 (PROVED) |
| R-02 | "Copy succeeded" taken as proof: restored DB differs from source (sequence values, dropped constraints, encoding) | DB | L | H | Per-table `count(*)` comparison, `pg_restore --list` vs source, `verify_schema()`/`check_integrity()` on target, `qevik_backup.sh --verify-only` | Use the existing restore-verify script on the target; compare table list (75 public tables PROVED in listing) and row counts for every table; compare `pg_dump --schema-only` diff | Rollback unaffected | `qevik_backup.sh:52-69` verify logic (PROVED repo) |
| R-03 | Schema drift: target boots a newer/older kernel than the one that last wrote the source DB; `init_db()` mutates the restored schema (renames, `DROP TABLE IF EXISTS atlas_campaigns`) | `db.py`, deploy SHA | M | H | Compare `DEPLOYED_SHA` on both hosts; schema-only diff before first boot | Deploy **exactly** the SHA deployed on the old host (`346076b` or later, never earlier); run `init_db()` once on the restored DB during Phase 7 and diff schema | A DB written by a newer kernel cannot be down-migrated → rollback must restore the **pre-cutover dump**, not reverse-sync | `db.py:870,879,1058,1073,1394` renames/drops; no migration tool (PROVED repo) |
| R-04 | Non-Postgres state forgotten: `vault.json`, `credentials.jsonl`, `missions.jsonl` (archive), `reports/`, `evidence/`, `jobs/`, `prospects/…`, `/srv/sites` are backed up by nothing today | `/var/lib/qevik`, `/srv/sites` | M | H | Manifest (sha256 per file) generated on source and verified on target | Data inventory S1–S11, W1 with per-tree manifests; Phase 6 script produces and compares manifests | Rollback unaffected (old host retains) | backup script dumps Postgres only (PROVED); dirs listed in `prod-storage.txt` |
| R-05 | Vault unreadable on target if `QEVIK_VAULT_MASTER_KEY` differs (`vault.json` 2 B suggests empty, but not proven) | credentials | L | M | `/api/health` "vault sealed" component; a controlled read of a known record | Owner decision O4 before Phase 6; test on target in Phase 7 | none | `vault.json` 2 B (PROVED); vault sealing (PROVED repo) |
| R-06 | Asset bytes referenced by `atlas_assets` rows do not exist anywhere (ephemeral `$TMPDIR/atlas-assets`) — discovered as "loss" after migration | Atlas assets | H (already true) | L | Count `atlas_assets` rows; attempt one fetch on old host | Document as **pre-existing** condition; not a migration loss | none | `composition_root.py:116`, `PrivateTmp=true` (PROVED repo) |
| R-07 | Heartbeat table (72 MB, growing) and `atlas_business_events` ledger grow during the window and slow the delta dump | DB | L | L | dump duration measured in Phase 6 | Dump is 19.6 MB compressed / ~seconds; acceptable | none | `prod-postgres.txt` sizes |

## B. Cutover and traffic

| ID | Risk | Component | L | I | Detection | Mitigation | Rollback implication | Evidence |
|---|---|---|---|---|---|---|---|---|
| R-08 | Cloudflare origin change is the **only** cutover lever and is manual; a typo or wrong record (4 names) sends traffic to the wrong place | Cloudflare DNS | M | H | Post-change: `curl --resolve` from devloop-01 and from a third vantage; `cf-ray` + `X-Qevik-Host` headers; `/health` on both apps | Owner changes all four A/AAAA records in one sitting from a pre-written checklist; agent verifies within 60 s; TTL is Cloudflare-proxied (instant) | Rollback = same manual change back; rehearse the change on a non-protected test name first (e.g. `migrate-test.qevik.ai`) | NS at Cloudflare, 4 proxied names (PROVED `dig @1.1.1.1`) |
| R-09 | Origin TLS: Cloudflare (Full/strict — INFERRED) rejects the target's certificate if LE certs are neither copied nor issued yet | Caddy / LE | M | H | 526/525 from Cloudflare; `curl --resolve` with `-k` vs without | Either copy `/var/lib/caddy` (O9) **or** issue certs on the target before cutover by pointing a temporary hostname at it; do not rely on HTTP-01 succeeding *during* cutover | If certs were copied, rollback unaffected; if re-issued, LE rate limits (5 dup certs/week) could block a second re-issue on rollback → copy is safer | `disable_tlsalpn_challenge`, HTTP-01 (PROVED Caddyfile); certs in `/var/lib/caddy` (PROVED) |
| R-10 | Cloudflare zone settings (SSL mode, WAF, cache, page rules) are UNKNOWN; something depends on the old origin IP (e.g. an Origin Rule or IP allow-list) | Cloudflare | L | M | Owner review of zone settings in Phase 1 | Owner screenshots/exports zone settings (no secrets) in Phase 1 | unaffected | UNKNOWN #2 in inventory |
| R-11 | Google Places key IP-restricted to old IP → market scan and discovery fail silently on the target | Places | **H (certain unless rotated)** | M | market-scan journal, `latest.json` mtime; `GET /api/health` research components | O6: new key restricted to target IP, entered in Phase 3; tested in Phase 7 | none | `00_PROJECT_STATE.md:36` (PROVED doc) |
| R-12 | Hard-coded `2.28.62.83` in 12 repo files: deploy tooling, DevLoop gates and `cloudflare.py` keep targeting the old host after cutover; a later ADR-0010 deploy lands on the decommissioned box or fails | repo | **H (certain)** | H | grep before Phase 4; `deploy_control.sh --rehearse` against the target | Phase 4 repo change (owner-reviewed, owner-pushed) parameterising or updating the IP; rehearse mode proves the target | Rollback needs the old value again → make the host configurable (`TARGET` env already exists in `deploy_control.sh:67`) rather than hard-swapping | inventory §14.2 item 1 (PROVED repo) |
| R-13 | `/health` returns 200 while the system is not actually working (liveness only; `/api/health` is the posture probe; workers may be crash-looping on DB) | validation | M | M | Use `/api/health` components, `atlas_workers` heartbeats (5 rows fresh), a test mission end-to-end, a test publish to `/srv/sites` | Phase 7 validation checklist below; never accept `/health` alone | — | health endpoints (PROVED repo) |

## C. Host and platform

| ID | Risk | Component | L | I | Detection | Mitigation | Rollback implication | Evidence |
|---|---|---|---|---|---|---|---|---|
| R-14 | Reboot survival of the full unit set has **never been exercised**; the old host has a pending reboot and 54 upgradable packages; the target will be rebooted at least once during provisioning | systemd | M | H | `systemctl list-unit-files --state=enabled`; reboot the target in Phase 7 and verify all 7 units + 2 timers + caddy + postgres come back | Enable every unit explicitly; verify ordering (`Requires=postgresql` only on api); reboot test is a Phase 7 success criterion | Rebooting the **old** host is out of scope until decommission (it is the rollback target) | `/var/run/reboot-required` (PROVED); PROJECT_STATE:74-76 |
| R-15 | Provisioning script gap: `bootstrap_qevik_server.sh` installs only `qevik-api`; slice + drop-in only in `recover_qevik_server.sh`; Caddyfile only via `deploy_console.sh` → a hand-assembled target drifts from the old host | provisioning | H | M | Diff of `/etc/systemd/system/qevik-*`, `/etc/caddy/Caddyfile`, drop-ins between hosts (sha256) | Phase 4 produces a written, reviewed provisioning checklist (or an updated script — implementation, needs approval) and Phase 7 diffs the results | none | inventory §14.2 items 2–4 |
| R-16 | Python dependency drift: no lock file; `pip install -e .[dev]` on the target resolves newer packages; playwright not declared | venv | M | M | `pip freeze` diff old vs new; test suite subset; Playwright smoke | Capture `pip freeze` on old host (Phase 4), install with `-c constraints.txt`; install playwright + browsers explicitly, pin Chromium 1234 | none | `pyproject.toml` ranges (PROVED repo) |
| R-17 | Ubuntu 26.04 image on Hetzner differs from the old host's package set (PG 18.6, Caddy 2.11.4) → different minor versions | packages | L | L | `dpkg -l` diff | Same distro release; accept minor drift; PG ≥ 18 mandatory | none | `prod-security-misc.txt` |
| R-18 | Resource sizing: old host is 4 vCPU/7.6 GiB with 1.7 GiB used and no swap; workers are uncapped; Chromium bursts (`QEVIK_MAX_BROWSERS=2`, capped at 2 per `feedback_browser_e2e_host_limit`) | sizing | L | M | `free`, cgroup memory events, OOM in journal | Target ≥ old host; add swap or `MemoryMax` on workers (Phase 1 decision) | none | utilisation snapshot §12 (PROVED single sample); peak UNKNOWN |
| R-19 | Old-host security posture carried over: `PasswordAuthentication yes`, fail2ban inactive, ~6.2k brute-force/day, shared SSH key with Naml, `credentials.jsonl` 0644, DSN in argv | security | H (if replicated) | M | sshd effective config, `ss`, `ps` | Phase 3: key-only sshd, fail2ban or Hetzner firewall for 22, dedicated key, 0600 on state files, no DSN on argv | none | `prod-security-misc.txt` (PROVED) |
| R-20 | Emergency door `:8443` is unreachable today (ufw) although docs call it the way back in if Cloudflare breaks → during cutover the only origin-direct check is from SSH/loopback | access | M (already true) | M | second-vantage port test | Decide in Phase 1: keep blocked (recommended) and rely on SSH + `curl --resolve` on loopback, or allow 8443 from owner IPs only | none | PROVED second vantage |
| R-21 | Hetzner-level unknowns: no snapshot/backup add-on verified, project/firewall unknown; a snapshot of the old host before cutover would be the cheapest full rollback but may not be enabled | Hetzner | M | M | owner console check (Phase 1) | Owner enables backups/snapshot for the **old** server before Phase 9 if available (owner action; costs 20 % of server price) | Improves rollback | UNKNOWN #1 |

## D. Process and governance

| ID | Risk | Component | L | I | Detection | Mitigation | Rollback implication | Evidence |
|---|---|---|---|---|---|---|---|---|
| R-22 | ~~Scope creep: production workloads placed on `qevik-devloop-01` "because it is already there"~~ **Re-scoped 2026-09-03 (D-R-1):** server 164307556 *is* the production target by owner decision after a read-only assessment; the residual risk is the inverse — **DevLoop installed on the production server later** | architecture | L | H | any `devloop` user, `devloop-driver` unit, `claude`/`codex` binary or `/srv/devloop` on `qevik-prod-01` | ADR-0011 amendment: DevLoop executor = a future separate server; Phase 10/11 checks include the absence of DevLoop artefacts | — | `DEVLOOP01_SUITABILITY_ASSESSMENT.md`; owner D-R-1 |
| R-23 | Secrets leak through the migration process itself (rsync of env files, `cat` for verification, DSN on argv, pasted in chat) | secrets | M | H | grep of session transcripts/task evidence for DSN patterns | Handling rules §5 of the secret inventory; env files are owner-created on the target; agent verifies names/modes only | — | earlier-session `ps` leak (OBSERVED) |
| R-24 | Decommissioning the old host before stabilisation destroys the rollback path and the only historical backups | decommission | L | H | — | Phase 11 gated on Phase 10 success criteria + owner approval; take a final full backup off-host before deletion | This *is* the rollback path | backups local-only (PROVED) |
| R-26 | **Second vantage point lost (U16):** devloop-01 was the plan's independent probe source; after D-R-1 it is the target itself, so a probe from it proves nothing about reachability | verification | H (certain) | M | probes run only from the target or the Mac | `qevik-core-01` runs read-only `curl`/`nc`/`dig` under an explicit AR-4 carve-out (reads only) + an external HTTPS checker for Phases 7–10; after Phase 11 the D-P uptime check is the external vantage | none | Mac egress unreliable (OBSERVED) |
| R-25 | DevLoop resumes (queue still holds 5 tasks) and deploys to the old host mid-migration | DevLoop | L | H | driver process on Mac; `DEPLOYED_SHA` change on old host | DevLoop stays paused for the whole migration (owner's standing instruction); `deploy_control.sh` `TARGET` is left pointing at the old host until Phase 4 change is approved | — | pause instruction; queue state |
| R-26 | Documentation drift: `00_PROJECT_STATE.md`, `cloudflare.py`, ADR-0011 measurements all name the old IP/host; after cutover, docs mislead the next operator | docs | H | L | grep for `2.28.62.83` | Phase 10 doc update (host-independent wording; record new IP once) | — | inventory §14 |

## E. Security findings surfaced by discovery (not migration risks, but must not be replicated)

| ID | Finding | Evidence | Action |
|---|---|---|---|
| F-1 | Two stale root `verify_recurrence.py` processes and 7 watcher loops carry the production DSN on argv (visible to any local user via `ps`) | `prod-services.txt` (redacted) | Do not replicate; rotate DB password (K1); terminate on old host only as part of Phase 11 or with owner approval |
| F-2 | `credentials.jsonl` is world-readable (0644) | `prod-dns-net-misc.txt:45` | 0600 on target |
| F-3 | sshd accepts passwords; fail2ban inactive; ~6.2k attempts/day | `prod-security-misc.txt`; ADR-0011:84 | Key-only on target |
| F-4 | Same SSH private key for Naml and Qevik hosts | ADR-0011:32 | Dedicated key (O2) |
| F-5 | Google Places key exposed in a screenshot; rotation open since Aug | `00_PROJECT_STATE.md:390-391` | Rotate (O6) |
| F-6 | Backups local-only; 8-day silent failure history; no alerting | `qevik_backup.sh`; PROJECT_STATE:432-438 | **Off-host copy + failure marker IMPLEMENTED on target 2026-09-03** (`OFFSITE_BACKUP.md`); `/api/health` component still T9 |
| F-7 | Public 8080 routes reached via `/control/*`: whether **all** are auth-gated is UNKNOWN | repo UNKNOWN #10 | Not a migration item; flag for product |
