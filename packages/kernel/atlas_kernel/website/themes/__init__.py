"""Themes — how content becomes markup.

One theme family in M015, per the approved scope. A theme is a pure function
from ``SiteContent`` to a file map, and must be deterministic: same content in,
same bytes out.
"""

__all__: list[str] = []
